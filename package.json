{
  "name": "maintenance-service-api",
  "version": "1.0.0",
  "description": "REST API for renewable energy equipment maintenance",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "uuid": "^14.0.2"
  }
}